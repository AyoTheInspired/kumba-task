import React from "react";
import styled from "styled-components";
import { Container, Row } from "react-bootstrap";

function Orders() {
	return (
		<Container>
			<Row>
				<Section className="">the orders page</Section>
			</Row>
		</Container>
	);
}

export default Orders;

const Section = styled.section``;
